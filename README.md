## Truss


## Usage

```
./truss --help
A longer description here.

Usage:
  truss [command]

Available Commands:
  help        Help about any command
  init        Initialize a new truss application

Flags:
      --compute string             compute target - Options: func, container
      --config string              config file (default is $HOME/.truss.yaml)
      --devops                     create registry
  -h, --help                       help for truss
      --input-source-name string   name of the input source
      --messaging string           messaging platform - Options: grpc, servicebus, json
      --name string                name of the appliction
      --output-sink-name string    name of the output sink

Use "truss [command] --help" for more information about a command.
```